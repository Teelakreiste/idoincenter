import { BrokenImgDirective } from './broken-img.directive';

describe('BrokenImgDirective', () => {
  it('should create an instance', () => {
    const directive = new BrokenImgDirective();
    expect(directive).toBeTruthy();
  });
});
